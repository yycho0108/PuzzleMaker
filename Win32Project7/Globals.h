#pragma once
#include <Windows.h>

extern int brdWidth, brdHeight;
extern int imgWidth, imgHeight;
extern int tileWidth, tileHeight;
extern int wndWidth, wndHeight;
extern int twSmall, thSmall;
extern HWND hMainWnd;